spring:
  data:
    mongodb:
      host: localhost
      port: 27017
      database: moviemang
      username: root
      password: root-pass
  jpa:
    open-in-view: false
    generate-ddl: true
    show-sql: true
    hibernate:
      ddl-auto: none
    properties:
      format_sql: true
management:
  endpoint:
    health:
      enabled: true
      show-details: always
  endpoints:
    web:
      cors:
        allowed-methods: *
      exposure:
        include: '*'
  health:
    diskspace:
      enabled: true
